ScreenGuis are designed in the Studio editor and driven by `UIController`.

This folder is deliberately NOT mapped in `default.project.json`: if it were,
every Rojo sync would delete the ScreenGuis built by hand in Studio.
